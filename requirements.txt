easyocr
deep-translator
mss
pynput
numpy
# Optional: only needed for the Gemini translation engine
google-generativeai
python-dotenv
# Optional: only needed for the batch_ocr.py / batch_ocr_simple.py scripts
# (the main live reader no longer requires OpenCV)
opencv-python
pytesseract
